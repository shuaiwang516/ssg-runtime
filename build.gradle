plugins {
    id 'java'
    id 'application'
    id 'com.github.johnrengelman.shadow' version '7.0.0'
}

group 'org.zlab'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    implementation 'com.github.javaparser:javaparser-core:3.25.2'
    implementation 'com.github.javaparser:javaparser-symbol-solver-core:3.23.0' // replace with the current version
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.1'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.1'
    implementation 'com.fasterxml.jackson.core:jackson-core:2.12.5'
    implementation 'com.fasterxml.jackson.core:jackson-databind:2.13.3'
    implementation 'com.fasterxml.jackson.core:jackson-annotations:2.13.3'
    implementation 'info.picocli:picocli:4.6.1'

}

test {
    useJUnitPlatform()
}

shadowJar {
    archiveBaseName.set('dinv-monitor-shadow')
    archiveClassifier.set('')
    archiveVersion.set('')
    mergeServiceFiles()

    dependencies {
        exclude(dependency('com.github.javaparser:javaparser-core:3.25.2'))
        exclude(dependency('com.github.javaparser:javaparser-symbol-solver-core:3.23.0'))
    }

    include 'org/zlab/dinv/runtimechecker/Runtime*'
    include 'org/zlab/dinv/runtimechecker/Quant*'
}

application {
    mainClassName = 'org.zlab.dinv.visibility.Main'
}

task embedInv(type: JavaExec) {
    mainClass.set('org.zlab.dinv.runtimechecker.Main')
    classpath = sourceSets.main.runtimeClasspath
}

task modifiedFields(type: JavaExec) {
    mainClass.set('org.zlab.dinv.modifiedfields.Main')
    classpath = sourceSets.main.runtimeClasspath
}

task modifiedConfigs(type: JavaExec) {
    mainClass.set('org.zlab.dinv.diffconfig.ConfigMain')
    classpath = sourceSets.main.runtimeClasspath
}

task diffHandler(type: JavaExec) {
    mainClass.set('org.zlab.dinv.diffconfig.DiffHandlerMain')
    classpath = sourceSets.main.runtimeClasspath
}